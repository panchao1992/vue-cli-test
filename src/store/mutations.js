export default {
    /**
     * @param {param}: method param
     */
    defaultMed(state, param) {
        state.param = param
    },
    addCount(state, count) {
        state.count = count
    },
}